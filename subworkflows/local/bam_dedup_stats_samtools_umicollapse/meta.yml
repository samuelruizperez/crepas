# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/subworkflows/yaml-schema.json
name: "bam_dedup_stats_samtools_umicollapse"
description: |
  UMICollapse dedup (optionally splitting the BAM by chromosome first and merging back
  afterwards), index the deduplicated BAM file and run samtools stats, flagstat and idxstats
keywords:
  - umi
  - dedup
  - index
  - bam
  - sam
  - cram
components:
  - bam_split_by_chromosome
  - umicollapse
  - samtools/merge
  - samtools/index
  - samtools/stats
  - samtools/idxstats
  - samtools/flagstat
  - bam_stats_samtools
input:
  - ch_bam_index:
      description: |
        Input BAM file and its index
        Structure: [ val(meta), path(bam), path(index/csi) ]
  - ch_chrom_sizes:
      type: file
      description: |
        Chromosome sizes file (first two columns: chrom, size; e.g. a .fai file), required when
        skip_split_by_chrom is false
        Structure: [ val(meta), path(chrom_sizes) ]
  - skip_split_by_chrom:
      type: boolean
      description: |
        If false, split the BAM by chromosome before deduplication (one UMICollapse task per
        chromosome) and merge the deduplicated BAMs back together afterwards. If true,
        deduplicate the whole BAM in a single task.
      pattern: "true|false"
output:
  - bam:
      description: |
        UMI deduplicated BAM file (merged back together, when skip_split_by_chrom is false)
        Structure: [ val(meta), path(bam) ]
  - index:
      description: |
        Index of the deduplicated BAM file
        Structure: [ val(meta), path(index) ]
  - dedup_stats:
      description: |
        File containing UMICollapse deduplication stats
        Structure: [ val(meta), path(log) ]
  - stats:
      description: |
        File containing samtools stats output
        Structure: [ val(meta), path(stats) ]
  - flagstat:
      description: |
        File containing samtools flagstat output
        Structure: [ val(meta), path(flagstat) ]
  - idxstats:
      description: |
        File containing samtools idxstats output
        Structure: [ val(meta), path(idxstats) ]
authors:
  - "@MatthiasZepper"
  - "@samuelruizperez"
maintainers:
  - "@samuelruizperez"
