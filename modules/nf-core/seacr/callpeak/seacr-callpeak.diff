Changes in component 'nf-core/seacr/callpeak'
'modules/nf-core/seacr/callpeak/environment.yml' is unchanged
'modules/nf-core/seacr/callpeak/meta.yml' is unchanged
Changes in 'seacr/callpeak/main.nf':
--- modules/nf-core/seacr/callpeak/main.nf
+++ modules/nf-core/seacr/callpeak/main.nf
@@ -14,7 +14,10 @@
 
     output:
     tuple val(meta), path("*.bed"), emit: bed
-    path "versions.yml"           , emit: versions
+    tuple val("${task.process}"), val('seacr'), val('1.3'), topic: versions, emit: versions_seacr
+    // WARN: Version information not provided by tool on CLI. Please update this string when bumping container versions.
+    tuple val("${task.process}"), val('bedtools'), eval("bedtools --version | sed -e 's/bedtools v//g'"), topic: versions, emit: versions_bedtools
+    tuple val("${task.process}"), val('r-base'), eval("R --version 2>&1 | head -1 | sed 's/^.*R version //; s/ .*\$//'"), topic: versions, emit: versions_rbase
 
     when:
     task.ext.when == null || task.ext.when
@@ -23,19 +26,11 @@
     def args = task.ext.args ?: ''
     def prefix = task.ext.prefix ?: "${meta.id}"
     def function_switch = ctrlbedgraph ? "$ctrlbedgraph" : "$threshold"
-    def VERSION = '1.3' // WARN: Version information not provided by tool on CLI. Please update this string when bumping container versions.
     """
     SEACR_1.3.sh \\
         $bedgraph \\
         $function_switch \\
         $args \\
         $prefix
-
-    cat <<-END_VERSIONS > versions.yml
-    "${task.process}":
-        seacr: $VERSION
-        bedtools: \$(bedtools --version | sed -e "s/bedtools v//g")
-        r-base: \$(echo \$(R --version 2>&1) | sed 's/^.*R version //; s/ .*\$//')
-    END_VERSIONS
     """
 }

'modules/nf-core/seacr/callpeak/tests/main.nf.test' is unchanged
'modules/nf-core/seacr/callpeak/tests/nextflow.config' is unchanged
'modules/nf-core/seacr/callpeak/tests/main.nf.test.snap' is unchanged
************************************************************
