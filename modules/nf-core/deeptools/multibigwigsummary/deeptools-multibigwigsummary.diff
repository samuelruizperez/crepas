Changes in component 'nf-core/deeptools/multibigwigsummary'
Changes in 'deeptools/multibigwigsummary/main.nf':
--- modules/nf-core/deeptools/multibigwigsummary/main.nf
+++ modules/nf-core/deeptools/multibigwigsummary/main.nf
@@ -13,6 +13,7 @@
 
     output:
     tuple val(meta), path("*.npz"), emit: matrix
+    tuple val(meta), path("*.tsv"), emit: raw_counts, optional: true
     tuple val("${task.process}"), val('deeptools'), eval('multiBigwigSummary --version | sed "s/multiBigwigSummary //g"') , emit: versions_deeptools, topic: versions
 
     when:
@@ -22,6 +23,7 @@
     def args   = task.ext.args ?: ''
     def prefix = task.ext.prefix ?: "all_bigwig"
     def blacklist_cmd = blacklist ? "--blackListFileName ${blacklist}" : ""
+    def outrawcounts_cmd = args.contains('--outRawCounts') ? '' : "--outRawCounts ${prefix}.tsv"
     def label  = labels ? "--labels ${labels.join(' ')}" : ''
     """
     multiBigwigSummary bins \\
@@ -30,12 +32,14 @@
         --bwfiles ${bigwigs.join(' ')} \\
         --numberOfProcessors $task.cpus \\
         --outFileName ${prefix}.bigwigSummary.npz \\
+        ${outrawcounts_cmd} \\
         $blacklist_cmd
     """
-
+    
     stub:
     def prefix = task.ext.prefix ?: "all_bigwig"
     """
     touch ${prefix}.bigwigSummary.npz
+    touch ${prefix}.tsv
     """
 }

'modules/nf-core/deeptools/multibigwigsummary/environment.yml' is unchanged
'modules/nf-core/deeptools/multibigwigsummary/meta.yml' is unchanged
'modules/nf-core/deeptools/multibigwigsummary/tests/main.nf.test.snap' is unchanged
'modules/nf-core/deeptools/multibigwigsummary/tests/main.nf.test' is unchanged
************************************************************
