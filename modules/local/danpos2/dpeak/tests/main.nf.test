nextflow_process {

    name "Test Process DANPOS2_DPEAK"
    script "../main.nf"
    process "DANPOS2_DPEAK"

    tag "modules"
    tag "modules_local"
    tag "danpos2"
    tag "danpos2/dpeak"

    test("sarscov2 - bam - dpeak - no background") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    [ file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam', checkIfExists: true) ], // treatment
                    [], // treatment_input
                    [], // treatment_count
                    [], // control
                    [], // control_input
                    []  // control_count
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                    process.out.pooled_xls,
                    process.out.pooled_treat_wig,
                    process.out.findAll { key, val -> key.startsWith('versions') }
                    ).match() }
            )
        }

    }

    test("sarscov2 - bam - dpeak - with background") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    [ file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam', checkIfExists: true) ], // treatment
                    [ file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam', checkIfExists: true) ], // treatment_input
                    [], // treatment_count
                    [], // control
                    [], // control_input
                    []  // control_count
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(
                    process.out.pooled_xls,
                    process.out.pooled_treat_wig,
                    process.out.pooled_input_wig,
                    process.out.findAll { key, val -> key.startsWith('versions') }
                    ).match() }
            )
        }

    }

    test("sarscov2 - bam - dpeak - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    [ file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam', checkIfExists: true) ], // treatment
                    [], // treatment_input
                    [], // treatment_count
                    [], // control
                    [], // control_input
                    []  // control_count
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }

    }

}
