process {

    withName: 'SUBREAD_FEATURECOUNTS' {
        ext.args = "-F SAF -O -p"
    }

    withName: 'REPLISEQ_CLASSIFY_GENES' {
        // The sarscov2 test alignments are tiny, so accept any gene with at least one read
        ext.args = "--min_gene_reads 1"
    }

}
