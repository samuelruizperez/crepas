process {

    withName: 'DEEPTOOLS_MULTIBAMSUMMARY' {
        ext.args = "--binSize 1000"
    }

    withName: 'REPLISEQ_RTNORMALIZE' {
        ext.args = "--min_bin_reads 1"
    }

    withName: 'REPLISEQ_RT_DOMAINS' {
        ext.args = "--classification binary --undo_sd 0"
    }

}
