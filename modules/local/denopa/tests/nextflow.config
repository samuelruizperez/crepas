process {

    withName: SAMTOOLS_REHEADER {
        ext.args   = "-c 'sed \"s/SN:I\\t/SN:chr1\\t/\"'"
        ext.prefix = { "${meta.id}.reheadered" }
    }

    withName: SAMTOOLS_VIEW {
        ext.args   = '-f 2 -F 268'
        ext.prefix = { "${meta.id}.filtered" }
    }

}
