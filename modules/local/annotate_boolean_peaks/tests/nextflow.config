process {

    withName: SAMTOOLS_SORT {
        ext.args   = '-n'
        ext.prefix = { "${meta.id}.nsorted" }
        cpus       = 1
    }

    withName: MACS3_CALLPEAK {
        ext.args = '--nomodel --extsize 200 --pvalue 1'
    }

}
