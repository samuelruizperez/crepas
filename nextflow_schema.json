{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/grothlab/crepas/master/nextflow_schema.json",
    "title": "grothlab/crepas pipeline parameters",
    "description": "grothlab/crepas is a bioinformatics pipeline for the analysis of chromatin sequencing data (ChIP-seq, ATAC-seq, SCAR-seq, ChOR-seq, etc.).",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "required": ["outdir", "input"],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.(csv|tsv|yaml|json)$",
                    "schema": "assets/schema_input.json",
                    "exists": true,
                    "description": "Path to comma-separated file containing information about the samples in the experiment.",
                    "help_text": "You will need to create a design file with information about the samples in your experiment before running the pipeline. Use this parameter to specify its location. It has to be a comma-separated file with 5 columns, and a header row. See [usage docs](https://github.com/grothlab/crepas/blob/main/docs/usage.md).",
                    "fa_icon": "fas fa-file-csv"
                },
                "fragment_size": {
                    "type": "integer",
                    "description": "Estimated fragment size used to extend single-end reads.",
                    "fa_icon": "fas fa-chart-area",
                    "default": 250
                },
                "seq_platform": {
                    "type": "string",
                    "description": "Platform/technology used to produce the reads. Corresponds to the `PL` tag in the SAM/BAM file header.",
                    "help_text": "See the [SAM format specification](https://github.com/samtools/hts-specs/blob/master/SAMv1.pdf). Valid values: CAPILLARY, DNBSEQ (MGI/BGI), ELEMENT, HELICOS, ILLUMINA, IONTORRENT, LS454, ONT (Oxford Nanopore), PACBIO (Pacific Biosciences), SINGULAR, SOLID, and ULTIMA. This field should be omitted when the technology is not in this list (though the PM field may still be present in this case) or is unknown."
                },
                "seq_center": {
                    "type": "string",
                    "description": "Sequencing center information to be added to read group of BAM files.",
                    "fa_icon": "fas fa-synagogue"
                },
                "read_length": {
                    "type": "integer",
                    "description": "Read length used to calculate MACS3 genome size for peak calling if `--macs_gsize` isn't provided.",
                    "fa_icon": "fas fa-chart-area",
                    "enum": [50, 75, 100, 150, 200],
                    "default": 50
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
                    "fa_icon": "fas fa-folder-open"
                },
                "email": {
                    "type": "string",
                    "description": "Email address for completion summary.",
                    "fa_icon": "fas fa-envelope",
                    "help_text": "Set this parameter to your e-mail address to get a summary e-mail with details of the run sent to you when the workflow exits. If set in your user config file (`~/.nextflow/config`) then you don't need to specify this on the command line for every run.",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$"
                },
                "multiqc_title": {
                    "type": "string",
                    "description": "MultiQC report title. Printed as page header, used for filename if not otherwise specified.",
                    "fa_icon": "fas fa-file-signature"
                }
            }
        },
        "reference_genome_options": {
            "title": "Reference genome options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Reference genome related files and options required for the workflow.",
            "required": ["genome", "fasta", "gtf"],
            "properties": {
                "genome": {
                    "type": "string",
                    "description": "Name of reference genome (e.g., `GRCh38`, `hg19`, `mm10`).",
                    "fa_icon": "fas fa-book"
                },
                "fasta": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "pattern": "^\\S+\\.fn?a(sta)?(\\.gz)?$",
                    "exists": true,
                    "description": "Path to FASTA genome file.",
                    "help_text": "If you don't have the appropriate alignment index available this will be generated for you automatically based on this file. Combine with `--save_reference` to save alignment index for future runs.",
                    "fa_icon": "far fa-file-code"
                },
                "gtf": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "exists": true,
                    "pattern": "^\\S+\\.gtf(\\.gz)?$",
                    "description": "Path to GTF annotation file.",
                    "fa_icon": "fas fa-code-branch"
                },
                "gff": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "exists": true,
                    "pattern": "^\\S+\\.gff3?(\\.gz)?$",
                    "fa_icon": "fas fa-code-branch",
                    "description": "Path to GFF3 annotation file."
                },
                "bwa_index": {
                    "type": "string",
                    "description": "Path to directory or tar.gz archive for pre-built BWA index.",
                    "fa_icon": "fas fa-bezier-curve"
                },
                "bwa_mem2_index": {
                    "type": "string",
                    "description": "Path to directory or tar.gz archive for pre-built BWAMEM2 index.",
                    "fa_icon": "fas fa-bezier-curve"
                },
                "bowtie_index": {
                    "type": "string",
                    "format": "path",
                    "fa_icon": "fas fa-bezier-curve",
                    "description": "Path to directory or tar.gz archive for pre-built Bowtie index."
                },
                "bowtie2_index": {
                    "type": "string",
                    "format": "path",
                    "fa_icon": "fas fa-bezier-curve",
                    "description": "Path to directory or tar.gz archive for pre-built Bowtie2 index."
                },
                "chromap_index": {
                    "type": "string",
                    "format": "path",
                    "fa_icon": "fas fa-bezier-curve",
                    "description": "Path to directory or tar.gz archive for pre-built Chromap index."
                },
                "star_index": {
                    "type": "string",
                    "format": "path",
                    "fa_icon": "fas fa-bezier-curve",
                    "description": "Path to directory or tar.gz archive for pre-built STAR index."
                },
                "hisat2_index": {
                    "type": "string",
                    "format": "path",
                    "fa_icon": "fas fa-bezier-curve",
                    "description": "Path to directory or tar.gz archive for pre-built HISAT2 index."
                },
                "minimap2_index": {
                    "type": "string",
                    "format": "path",
                    "fa_icon": "fas fa-bezier-curve",
                    "description": "Path to directory or tar.gz archive for pre-built Minimap2 index."
                },
                "gene_bed": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "exists": true,
                    "pattern": "^\\S+\\.bed(\\.gz)?$",
                    "fa_icon": "fas fa-procedures",
                    "description": "Path to BED file containing gene intervals. This will be created from the GTF file if not specified."
                },
                "initiation_zones": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "exists": true,
                    "fa_icon": "fas fa-procedures",
                    "description": "Path to BED file containing initiation zones for SCAR-seq analysis."
                },
                "okseq_rfd_file": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "exists": true,
                    "pattern": "^\\S+\\.(txt|tsv|bed)(\\.gz)?$",
                    "fa_icon": "fas fa-procedures",
                    "description": "Path to GZIP-compressed OK-seq RFD text file. The path cannot contain spaces and must have extension '.txt.gz', '.tsv.gz' or '.bed.gz'"
                },
                "macs_gsize": {
                    "type": "number",
                    "description": "Effective genome size parameter required by MACS3.",
                    "help_text": "[Effective genome size](https://github.com/taoliu/MACS#-g--gsize) parameter required by MACS3.",
                    "fa_icon": "fas fa-arrows-alt-h"
                },
                "blacklist": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "exists": true,
                    "description": "Path to blacklist regions in BED format, used for filtering alignments.",
                    "help_text": "If provided, alignments that overlap with the regions in this file will be filtered out (see [ENCODE blacklists](https://sites.google.com/site/anshulkundaje/projects/blacklists)). The file should be in BED format.",
                    "fa_icon": "fas fa-book-dead"
                },
                "sparsebed": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "exists": true,
                    "description": "Path to BED file with sparse segments for Consenrich.",
                    "fa_icon": "fas fa-book-dead",
                    "help_text": "Defines the annotation file of 'sparse segments' (genomic regions) from the complement of an 'inflated' BED file of known active regions. See https://github.com/nolan-h-hamilton/Consenrich/blob/5e7f287d81ac4a9bc098ae29e825a5ded07af3b8/consenrich/consenrich.py#L58C54-L58C174"
                },
                "active_regions": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "exists": true,
                    "description": "Path to BED file with active regions for Consenrich.",
                    "fa_icon": "fas fa-book-dead",
                    "help_text": "Defines the annotation file of 'active regions' (genomic regions) for Consenrich. See https://github.com/nolan-h-hamilton/Consenrich/blob/5e7f287d81ac4a9bc098ae29e825a5ded07af3b8/consenrich/consenrich.py#L67"
                },
                "rocco_params": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Path to ROCCO parameters file.",
                    "fa_icon": "fas fa-file-alt",
                    "help_text": "CSV file containing chromosome-specific optimization parameters. Each supported genome has a custom `--params` file packaged with ROCCO, but a custom file has to be manually passed to ROCCO within this pipeline. Consider using this argument to set chromosome-specific budget and gamma values custom to your data and preferences. See the [ROCCO documentation](https://nolan-h-hamilton.github.io/ROCCO/#rocco-constants) for more details."
                },
                "splicesites": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Path to splice sites file for HISAT2.",
                    "help_text": "If using HISAT2 as the aligner, you can provide a list of known splice sites, which HISAT2 makes use of to align reads with small anchors. If this file is not provided, it will be generated from the GTF file.",
                    "fa_icon": "fas fa-code-branch"
                },
                "save_reference": {
                    "type": "boolean",
                    "default": true,
                    "description": "If generated by the pipeline save the BWA index in the results directory.",
                    "help_text": "If the BWA index is generated by the pipeline use this parameter to save it to your results folder. These can then be used for future pipeline runs, reducing processing times.",
                    "fa_icon": "fas fa-save"
                },
                "skip_gtf_index": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip indexing the GTF file with tabix before processing.",
                    "help_text": "Set this parameter to true to skip indexing the GTF file with tabix. This may be useful if your GTF file is already indexed or if you encounter issues with tabix.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "igenomes_base": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Directory / URL base for iGenomes references.",
                    "default": "s3://ngi-igenomes/igenomes/",
                    "fa_icon": "fas fa-cloud-download-alt",
                    "hidden": true
                },
                "igenomes_ignore": {
                    "type": "boolean",
                    "description": "Do not load the iGenomes reference config.",
                    "fa_icon": "fas fa-ban",
                    "hidden": true,
                    "help_text": "Do not load `igenomes.config` when running the pipeline. You may choose this option if you observe clashes between custom parameters and those supplied in `igenomes.config`.",
                    "default": true
                },
                "refgenie_ignore": {
                    "type": "boolean",
                    "description": "Whether to ignore the Refgenie reference genomes.",
                    "fa_icon": "fas fa-ban",
                    "hidden": true,
                    "help_text": "Do not use the Refgenie reference genomes, even if loaded before through config files.",
                    "default": true
                }
            }
        },
        "umi_extraction_and_deduplication_options": {
            "title": "UMI extraction and deduplication options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Options to adjust UMI extraction and deduplication criteria.",
            "properties": {
                "with_umi": {
                    "type": "boolean",
                    "default": false,
                    "description": "Set this parameter to enable UMI extraction and deduplication.",
                    "fa_icon": "fas fa-dna"
                },
                "skip_umi_extract": {
                    "type": "boolean",
                    "description": "Skip UMI extraction step.",
                    "help_text": "Use this flag to skip the UMI extraction/transfer step. This is useful if you have already extracted UMIs from your data but want to run the deduplication",
                    "fa_icon": "fas fa-dna",
                    "default": true
                },
                "umitools_extract_method": {
                    "type": "string",
                    "description": "UMI pattern to use. Can be either `string` (default) or `regex`.",
                    "help_text": "More details can be found in the [UMI-tools documentation](https://umi-tools.readthedocs.io/en/latest/reference/extract.html#extract-method).\n",
                    "fa_icon": "fas fa-dna",
                    "default": "string",
                    "enum": ["string", "regex"]
                },
                "umitools_bc_pattern": {
                    "type": "string",
                    "default": "NNNNNNNNN",
                    "fa_icon": "fas fa-barcode",
                    "help_text": "More details can be found in the [UMI-tools documentation](https://umi-tools.readthedocs.io/en/latest/reference/extract.html#extract-method).",
                    "description": "The UMI barcode pattern to use e.g. 'NNNNNN' indicates that the first 6 nucleotides of the read are from the UMI."
                },
                "umitools_bc_pattern2": {
                    "type": "string",
                    "default": "NNNNNNNNN",
                    "fa_icon": "fas fa-barcode",
                    "description": "The UMI barcode pattern to use if the UMI is located in read 2."
                },
                "umi_separator": {
                    "type": "string",
                    "description": "Separator used to separate the UMI from the read name.",
                    "fa_icon": "fas fa-dna",
                    "default": "_"
                },
                "umi_grouping_method": {
                    "type": "string",
                    "description": "Method to use to determine read groups by subsuming those with similar UMIs. All methods start by identifying the reads with the same mapping position, but treat similar yet nonidentical UMIs differently.",
                    "fa_icon": "fas fa-dna",
                    "default": "percentile",
                    "enum": ["unique", "percentile", "directional", "adjacency", "cluster"]
                },
                "umi_discard_read": {
                    "type": "integer",
                    "description": "After UMI barcode extraction discard either R1 or R2 by setting this parameter to `1` or `2`, respectively.",
                    "fa_icon": "fas fa-dna"
                },
                "get_dedup_stats": {
                    "type": "boolean",
                    "default": true,
                    "description": "Get deduplication statistics.",
                    "fa_icon": "fas fa-dna"
                },
                "umi_dedup_tool": {
                    "type": "string",
                    "default": "umitools",
                    "description": "UMI deduplication tool to use.",
                    "fa_icon": "fas fa-dna",
                    "enum": ["umicollapse", "umitools"]
                },
                "save_dedup_intermeds": {
                    "type": "boolean",
                    "description": "Save intermediate files from the UMI deduplication step.",
                    "fa_icon": "fas fa-save",
                    "default": true
                }
            }
        },
        "adapter_trimming_options": {
            "title": "Adapter trimming options",
            "type": "object",
            "fa_icon": "fas fa-cut",
            "description": "Options to adjust adapter trimming criteria.",
            "properties": {
                "clip_r1": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to remove bp from the 5' end of read 1 (or single-end reads).",
                    "fa_icon": "fas fa-cut"
                },
                "clip_r2": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to remove bp from the 5' end of read 2 (paired-end reads only).",
                    "fa_icon": "fas fa-cut"
                },
                "three_prime_clip_r1": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to remove bp from the 3' end of read 1 AFTER adapter/quality trimming has been performed.",
                    "fa_icon": "fas fa-cut"
                },
                "three_prime_clip_r2": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to remove bp from the 3' end of read 2 AFTER adapter/quality trimming has been performed.",
                    "fa_icon": "fas fa-cut"
                },
                "trim_full_length": {
                    "type": "boolean",
                    "default": true,
                    "description": "Instructs Trim Galore to search for the full length (33 bp) TruSeq (ChIP-seq, ChOR-seq, SCAR-seq) or Nextera (ATAC-seq) adapter sequences for both complement and reverse complements.",
                    "help_text": "The default (false) setting will mean that the auto-detect function of Trim Galore will be used. See [https://github.com/FelixKrueger/TrimGalore/blob/master/Docs/Trim_Galore_User_Guide.md#adapter-auto-detection](https://github.com/FelixKrueger/TrimGalore/blob/master/Docs/Trim_Galore_User_Guide.md#adapter-auto-detection)",
                    "fa_icon": "fas fa-cut"
                },
                "trim_nextseq": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to apply the `--nextseq=X` option, to trim based on quality after removing poly-G tails.",
                    "help_text": "This enables the Cutadapt `--nextseq-trim=3'CUTOFF` option via Trim Galore, which will set a quality cutoff (that is normally given with -q instead), but qualities of G bases are ignored. This trimming is in common for the NextSeq- and NovaSeq-platforms, where basecalls without any signal are called as high-quality G bases. Read the [Cutadapt documentation](https://cutadapt.readthedocs.io/en/stable/guide.html#nextseq-trim) for more details.",
                    "fa_icon": "fas fa-cut",
                    "default": 20
                },
                "trim_stringency": {
                    "type": "integer",
                    "default": 3,
                    "description": "Instructs Trim Galore to use this stringency (minimum overlap) when searching for adapter sequences.",
                    "help_text": "In other words, to reduce the number of falsely trimmed bases, the alignment algorithm in Trim Galore requires that at least this number of bases of the adapter are aligned to the read. This parameter corresponds to the `--stringency` option in Trim Galore and the `--overlap` option in [Cutadapt](https://cutadapt.readthedocs.io/en/stable/guide.html#random-matches).",
                    "fa_icon": "fas fa-cut"
                },
                "trim_error_rate": {
                    "type": "number",
                    "default": 0.2,
                    "description": "Instructs Trim Galore to use this error rate when trimming.",
                    "fa_icon": "fas fa-cut"
                },
                "trim_q_cutoff": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to use this quality cutoff when trimming.",
                    "fa_icon": "fas fa-cut"
                },
                "trim_minimum_length": {
                    "type": "integer",
                    "default": 5,
                    "description": "Instructs Trim Galore to discard reads that became shorter than this length after adapter or quality trimming. A value of 0 effectively disables this behaviour."
                },
                "trim_maximum_length": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to discard reads that are longer than this length after adapter or quality trimming."
                },
                "skip_trimming": {
                    "type": "boolean",
                    "description": "Skip the adapter trimming step.",
                    "help_text": "Use this if your input FastQ files have already been trimmed outside of the workflow or if you're very confident that there is no adapter contamination in your data.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "save_trimmed": {
                    "type": "boolean",
                    "description": "Save the trimmed FastQ files in the results directory.",
                    "help_text": "By default, trimmed FastQ files will not be saved to the results directory. Specify this flag (or set to true in your config file) to copy these files to the results directory when complete.",
                    "fa_icon": "fas fa-save"
                },
                "min_trimmed_reads": {
                    "type": "integer",
                    "default": 1,
                    "description": "Minimum number of reads required after trimming.",
                    "fa_icon": "fas fa-cut"
                }
            }
        },
        "hard_trimming_options": {
            "title": "Hard trimming options",
            "type": "object",
            "fa_icon": "fas fa-cut",
            "description": "Options to adjust hard trimming criteria.",
            "properties": {
                "hardtrim5_length": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to hard-trim (shorten) reads to this length from their 3' end. This is performed after adapter/quality trimming.",
                    "fa_icon": "fas fa-cut"
                },
                "hardtrim3_length": {
                    "type": "integer",
                    "description": "Instructs Trim Galore to hard-trim (shorten) reads to this length from their 5' end. This is performed after adapter/quality trimming.",
                    "fa_icon": "fas fa-cut"
                },
                "save_hardtrimmed": {
                    "type": "boolean",
                    "default": true,
                    "description": "Whether to save the hard-trimmed reads (FASTQs) in the output directory."
                }
            }
        },
        "alignment_options": {
            "title": "Alignment options",
            "type": "object",
            "fa_icon": "fas fa-map-signs",
            "description": "Options to adjust parameters and filtering criteria for read alignments.",
            "properties": {
                "aligner": {
                    "type": "string",
                    "default": "chromap",
                    "description": "Specifies the alignment algorithm to use. The available options are `bwa`, `bowtie2`, `chromap`, `star`, and `hisat2`.",
                    "fa_icon": "fas fa-map-signs",
                    "enum": ["bwa", "bwamem2", "bowtie", "bowtie2", "chromap", "star", "hisat2", "strobealign", "minimap2"]
                },
                "bwa_min_score": {
                    "type": "integer",
                    "description": "Don't output BWA MEM alignments with score lower than this parameter.",
                    "fa_icon": "fas fa-hand-paper"
                },
                "bwamem2_min_score": {
                    "type": "integer",
                    "description": "Don't output BWAMEM2 alignments with score lower than this parameter.",
                    "fa_icon": "fas fa-hand-paper"
                },
                "sort_bam": {
                    "type": "boolean",
                    "default": true,
                    "description": "Should the BAM files be sorted by coordinate? Only relevant if Bowtie2 or BWA are used as aligners.",
                    "fa_icon": "fas fa-sort-numeric-down"
                },
                "save_align_intermeds": {
                    "type": "boolean",
                    "description": "Save the intermediate BAM files from the alignment step.",
                    "help_text": "By default, intermediate BAM files will not be saved. The final BAM files created after the appropriate filtering step are always saved to limit storage usage. Set this parameter to also save other intermediate BAM files.",
                    "fa_icon": "fas fa-save"
                },
                "save_unaligned": {
                    "type": "boolean",
                    "fa_icon": "fas fa-save",
                    "description": "Where possible, save unaligned reads from either STAR or HISAT2 to the results directory.",
                    "help_text": "This may either be in the form of FastQ or BAM files depending on the options available for that particular tool."
                },
                "map_n_multimappers": {
                    "type": "integer",
                    "fa_icon": "fas fa-dice-d20",
                    "description": "Number of multimappers to map."
                },
                "align_end_to_end": {
                    "type": "boolean",
                    "default": true,
                    "description": "Wheter to perform end-to-end alignment (only relevant for Bowtie2 and STAR). If set to false, local alignment will be performed.",
                    "fa_icon": "fas fa-map-signs"
                },
                "minimap2_preset": {
                    "type": "string",
                    "default": "sr",
                    "description": "Preset to use for minimap2 alignment. See the [minimap2 documentation](https://lh3.github.io/minimap2/minimap2.html) for more details.",
                    "fa_icon": "fas fa-map-signs",
                    "enum": ["sr"]
                },
                "minimap2_kmer_length": {
                    "type": "integer",
                    "default": 21,
                    "description": "K-mer length to use for minimap2 indexing and alignment. See the [minimap2 documentation](https://lh3.github.io/minimap2/minimap2.html) for more details.",
                    "fa_icon": "fas fa-map-signs"
                },
                "minimap2_min_win_size": {
                    "type": "integer",
                    "default": 11,
                    "description": "Minimizer window size to use for minimap2 indexing and alignment. See the [minimap2 documentation](https://lh3.github.io/minimap2/minimap2.html) for more details.",
                    "fa_icon": "fas fa-map-signs"
                }
            }
        },
        "spikein_options": {
            "title": "Spike-in options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Options to adjust spike-in splitting criteria.",
            "properties": {
                "spikein_genome": {
                    "type": "string",
                    "description": "Name of the spike-in genome (if used in the experiment). The spike-in (exogenous) chromosome names in the `--fasta` and `<aligner>_index` files should have the format `<chromosome_name>_<spikein_genome>`, e.g. `chr1_dm6`.",
                    "fa_icon": "fas fa-dna"
                },
                "save_spikein_intermeds": {
                    "type": "boolean",
                    "default": false,
                    "description": "Save intermediate files during spike-in splitting and filtering of BAMs. These include the split and filtered BAM files before coordinate-sorting.",
                    "fa_icon": "fas fa-save"
                }
            }
        },
        "multimapper_allocation_options": {
            "title": "Multimapper allocation options",
            "type": "object",
            "fa_icon": "fas fa-dice-d20",
            "description": "Options to adjust multimapper allocation criteria.",
            "properties": {
                "multimap_allocation_method": {
                    "type": "string",
                    "description": "Method to allocate multimappers. Options are `allo`, `chromap`, and `mmr`. Not setting this parameter means that multimappers will not be allocated (the default).",
                    "fa_icon": "fas fa-dice-d20",
                    "enum": ["allo", "chromap", "mmr"]
                },
                "allo_mixed_cnn": {
                    "type": "boolean",
                    "default": true,
                    "description": "When using Allo for multimapper allocation, use the CNN trained on histone ChIP-seq datasets with mixed peaks, otherwise use the default (narrow). See [allo](https://github.com/seqcode/allo?tab=readme-ov-file#options) for more details.",
                    "fa_icon": "fas fa-dice-d20"
                },
                "save_allocation_intermeds": {
                    "type": "boolean",
                    "default": true,
                    "description": "Save the intermediate BAM files from the multimapper allocation step.",
                    "fa_icon": "fas fa-save"
                },
                "allocate_exogenous": {
                    "type": "boolean",
                    "default": true,
                    "description": "Whether to also allocate multimappers in the spike-in (exogenous) BAM files.",
                    "fa_icon": "fas fa-dice-d20"
                }
            }
        },
        "alignment_shifting_options": {
            "title": "Alignment shifting options",
            "type": "object",
            "fa_icon": "fas fa-map-signs",
            "description": "Options to adjust alignment shifting criteria.",
            "properties": {
                "save_shifting_intermeds": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to save the intermediate files from the alignment shifting (only done on ATAC-seq data).",
                    "fa_icon": "fas fa-save"
                }
            }
        },
        "filtering_options": {
            "title": "Filtering options",
            "type": "object",
            "fa_icon": "fas fa-filter",
            "description": "Options to adjust filtering criteria for BAM files.",
            "properties": {
                "skip_flT3": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to skip the flT3 filtering step (filtering by mapping quality, which also removes multimapping reads in most cases).",
                    "fa_icon": "fas fa-filter"
                },
                "skip_flTbl": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to skip the blacklist filtering step. This serves to skip the removal of blacklisted regions from the BAM files even if a blacklist BED file is provided.",
                    "fa_icon": "fas fa-filter"
                }
            }
        },
        "coverage_and_normalization_options": {
            "title": "Coverage and normalization options",
            "type": "object",
            "fa_icon": "fas fa-chart-area",
            "description": "Options to adjust coverage and normalization criteria.",
            "properties": {
                "skip_srpm": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to skip the SRPM normalization step. See the [output documentation](https://github.com/grothlab/crepas/blob/dev/docs/output.md#normalized-bigwig-files) for more details.",
                    "fa_icon": "fas fa-chart-area"
                },
                "skip_cisrpm": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to skip the CISRPM normalization step. See the [output documentation](https://github.com/grothlab/crepas/blob/dev/docs/output.md#normalized-bigwig-files) for more details.",
                    "fa_icon": "fas fa-chart-area"
                },
                "skip_cisrpmsoi": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to skip the CISRPM SOI step. See the [output documentation](https://github.com/grothlab/crepas/blob/dev/docs/output.md#normalized-bigwig-files) for more details.",
                    "fa_icon": "fas fa-chart-area"
                },
                "soi_min_count": {
                    "type": "number",
                    "default": 0.00001,
                    "description": "Minimum CISRPM value required in a bin to be included in the signal over input (SOI) calculation.",
                    "fa_icon": "fas fa-chart-area"
                },
                "coverage_extend_reads": {
                    "type": "integer",
                    "default": 250,
                    "description": "This parameter allows the extension of reads to fragment size. If set, each read is extended, without exception.",
                    "help_text": "Single-end: Requires a user specified value for the final fragment length. Reads that already exceed this fragment length will not be extended. Paired-end: Reads with mates are always extended to match the fragment size defined by the two read mates. Unmated reads, mate reads that map too far apart (>4x fragment length) or even map to different chromosomes are treated like single-end reads. The input of a fragment length value is optional. If no value is specified, it is estimated from the data (mean of the fragment size of all mate reads). Read the [deepTools documentation](https://deeptools.readthedocs.io/en/develop/content/tools/bamCoverage.html#read-processing-options) for more details.",
                    "fa_icon": "fas fa-chart-area"
                },
                "coverage_bin_size": {
                    "type": "integer",
                    "default": 250,
                    "description": "Size of the bins, in bases, for the output of the coverage calculation.",
                    "fa_icon": "fas fa-chart-area"
                },
                "use_flT2_as_total_ref": {
                    "type": "string",
                    "default": "H3K9me3,H3K27me3",
                    "description": "Comma-separated list of histone marks for which downsampling or normalization factors should be calculated using the total number of reads before the quality filtering in flT3. The total mapped reads after flT1 will be used if flT2 was not performed (i.e., when there is no spike-in splitting). For marks/antibodies not in this list the total mapped reads after flT3 will be used for normalization factor calculation.",
                    "fa_icon": "fas fa-chart-area"
                },
                "save_coverage_intermeds": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to save the intermediate files from the coverage calculation step. These include the coverage files in bedGraph format.",
                    "fa_icon": "fas fa-save"
                },
                "min_reads_for_norm": {
                    "type": "integer",
                    "default": 1,
                    "description": "Minimum number of total mapped reads required to include a BAM in any normalization. For example, this prevents having an empty exogenous input control BAM when no reads map to the spike-in genome.",
                    "fa_icon": "fas fa-chart-area"
                },
                "skip_rpm_lfc": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to skip the calculation of the log2ratio between RPM-normalized IP and RPM-normalized input bigWig files.",
                    "fa_icon": "fas fa-chart-area"
                },
                "skip_bw_average": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to skip the generation of average bigWig files across replicates for each sample group.",
                    "fa_icon": "fas fa-chart-area"
                }
            }
        },
        "downsampling_options": {
            "title": "Downsampling options",
            "type": "object",
            "fa_icon": "fas fa-compress-arrows-alt",
            "description": "Options to adjust downsampling criteria.",
            "properties": {
                "bam_downsampling_method": {
                    "type": "string",
                    "description": "Method to downsample BAM files. Leave empty to disable downsampling.",
                    "help_text": "The `min_endo_across` method will downsample all IPs and inputs of the same antibody to the minimum number of endogenous reads across all IPs and inputs of the same antibody.\nThe `min_exo_across` method will downsample all IPs and inputs of the same antibody to the minimum number of exogenous reads across all IPs and inputs of the same antibody.\nThe `min_endo_by_type` method will downsample all IPs to the minimum number of endogenous reads across all IPs of the same antibody, and all the inputs to the minimum number of endogenous reads across all inputs of the same antibody.\nThe `min_exo_by_type` method will downsample all IPs to the minimum number of exogenous reads across all IPs of the same antibody, and all the inputs to the minimum number of exogenous reads across all inputs of the same antibody.",
                    "fa_icon": "fas fa-compress-arrows-alt",
                    "enum": ["min_endo_across", "min_exo_across", "min_endo_by_type", "min_exo_by_type"]
                },
                "downsampling_endo_threshold": {
                    "type": "integer",
                    "default": 10000000,
                    "description": "Threshold of endogenous mapped reads for downsampling. For downsampling methods that search for the minimum endogenous mapped reads across a set of samples, this parameter will determine the lower limit of that minimum. In other words, the minimum mapped endogenous reads should be the minimum number above the `downsampling_endo_threshold`. If there are no totals above this threshold, then the (true) minimum is used.",
                    "fa_icon": "fas fa-compress-arrows-alt"
                },
                "downsampling_exo_threshold": {
                    "type": "integer",
                    "default": 300000,
                    "description": "Threshold of exogenous mapped reads for downsampling. For downsampling methods that search for the minimum exogenous mapped reads across a set of samples, this parameter will determine the lower limit of that minimum. In other words, the minimum mapped exogenous reads should be the minimum number above the `downsampling_exogenous_threshold`. If there are no totals above this threshold, then the (true) minimum is used.",
                    "fa_icon": "fas fa-compress-arrows-alt"
                },
                "bam_downsampling_strategy": {
                    "type": "string",
                    "default": "Chained",
                    "description": "Strategy to downsample BAM files. See [Picard](https://gatk.broadinstitute.org/hc/en-us/articles/360036431292-DownsampleSam-Picard) for more details.",
                    "fa_icon": "fas fa-compress-arrows-alt",
                    "enum": ["ConstantMemory", "HighAccuracy", "Chained"]
                },
                "bam_downsampling_accuracy": {
                    "type": "number",
                    "default": 1e-6,
                    "description": "Accuracy of downsampling BAM files. See [Picard](https://gatk.broadinstitute.org/hc/en-us/articles/360036431292-DownsampleSam-Picard) for more details.",
                    "fa_icon": "fas fa-compress-arrows-alt"
                },
                "save_downsampling_intermeds": {
                    "type": "boolean",
                    "default": true,
                    "description": "Save the intermediate BAM files from the downsampling step.",
                    "fa_icon": "fas fa-save"
                }
            }
        },
        "tecounting_options": {
            "title": "TE counting options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Options to adjust transposable element (TE) counting criteria.",
            "properties": {
                "skip_te_counting": {
                    "type": "boolean",
                    "default": true,
                    "description": "Skip transposable element (TE) counting.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_telocal": {
                    "type": "boolean",
                    "default": true,
                    "description": "Skip local TE counting.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_tecount_gz": {
                    "type": "boolean",
                    "default": true,
                    "description": "Skip gzip compression of TEcount output files.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_telocal_gz": {
                    "type": "boolean",
                    "default": true,
                    "description": "Skip gzip compression of TElocal output files.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "te_counting_mode": {
                    "type": "string",
                    "default": "multi",
                    "description": "Method to use for TE counting. See [TEcount's documentation](https://github.com/mhammell-laboratory/TEtranscripts?tab=readme-ov-file#usage-1) for more details.",
                    "fa_icon": "fas fa-dna",
                    "enum": ["multi", "uniq"]
                },
                "te_counting_gene_gtf": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to a different gene GTF file for counting with TEcount and TElocal. If not provided, the main `--gtf` file will be used to generate a gene index suitable for TEcount or TElocal, unless the respective prebuilt gene index files are provided.",
                    "fa_icon": "fas fa-dna"
                },
                "tecount_gene_index": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to the prebuilt gene index file (.ind) for counting with TEcount. If not provided, this file in `--gtf` will be used to generate a gene index suitable for TEcount.",
                    "fa_icon": "fas fa-dna"
                },
                "telocal_gene_index": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to the prebuilt gene index file (.ind) for counting with TElocal. If not provided, this file in `--gtf` will be used to generate a gene index suitable for TElocal.",
                    "fa_icon": "fas fa-dna"
                },
                "te_gtf": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to the TE GTF file for counting with TEcount or TElocal. If not provided, this file will be used to generate a TE index suitable for TEcount or TElocal.",
                    "fa_icon": "fas fa-dna"
                },
                "tecount_te_index": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to the prebuilt TE index file (.ind) for counting with TEcount. If not provided, this file in `--te_gtf` will be used to generate a TE index suitable for TEcount.",
                    "fa_icon": "fas fa-dna"
                },
                "telocal_te_index": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to the prebuilt TE index file (.locInd) for local (instance) TE counting with TElocal. If not provided, this file in `--te_gtf` will be used to generate a TE index suitable for TElocal.",
                    "fa_icon": "fas fa-dna"
                }
            }
        },
        "peak_calling_options": {
            "title": "Peak calling options",
            "type": "object",
            "fa_icon": "fas fa-chart-area",
            "description": "Options to adjust peak calling criteria.",
            "properties": {
                "narrow_peak": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to call narrow peaks with MACS3 and Genrich. Default is to call broad peaks.",
                    "fa_icon": "fas fa-arrows-alt-h"
                },
                "peak_cutoff_method": {
                    "type": "string",
                    "default": "qvalue",
                    "description": "Whether to use q-value or p-value as the cutoff for peak calling.",
                    "fa_icon": "fas fa-sort-amount-down",
                    "enum": ["qvalue", "pvalue"]
                },
                "narrow_cutoff": {
                    "type": "number",
                    "default": 0.01,
                    "description": "Specifies narrow cutoff value (p-value or q-value) for MACS3 and Genrich peak calling.",
                    "fa_icon": "fas fa-hand-scissors"
                },
                "broad_cutoff": {
                    "type": "number",
                    "default": 0.1,
                    "description": "Specifies broad cutoff value (p-value or q-value) for MACS3 and Genrich peak calling.",
                    "fa_icon": "fas fa-hand-scissors"
                },
                "min_reps_consensus": {
                    "type": "integer",
                    "default": 1,
                    "description": "Number of biological replicates required from a given condition for a peak to contribute to a consensus peak.",
                    "help_text": "If you are confident you have good reproducibility amongst your replicates then you can increase the value of this parameter to create a 'reproducible' set of consensus peaks. For example, a value of 2 will mean peaks that have been called in at least 2 replicates will contribute to the consensus set of peaks, and as such peaks that are unique to a given replicate will be discarded.",
                    "fa_icon": "fas fa-sort-numeric-down"
                },
                "macs_extsize": {
                    "type": "integer",
                    "description": "Used to extend reads in 5'->3' direction to fix-sized fragments when processing ATAC-seq data, for which MACS3 bypasses building the shifting model (`--nomodel`). See [MACS3 documentation](https://macs3-project.github.io/MACS/docs/callpeak.html) for more details.",
                    "default": 250
                },
                "macs_slocal": {
                    "type": "integer",
                    "default": 1500,
                    "description": "Size of the small local region around peak regions to calculate the maximum lambda as local lambda.",
                    "help_text": "By default, MACS3 considers 1000 bp for small local region (`--slocal`). You can tweak this according to your project. Remember that if the region is set too small, a sharp spike in the input data may kill a significant peak. See [MACS3 documentation](https://macs3-project.github.io/MACS/docs/callpeak.html) for more details.",
                    "fa_icon": "fas fa-chart-area"
                },
                "macs_llocal": {
                    "type": "integer",
                    "default": 10000,
                    "description": "Size of the large local region around peak regions to calculate the maximum lambda as local lambda.",
                    "help_text": "By default, MACS3 considers 10000 bp for large local region (`--llocal`) which captures the bias from a long-range effect like an open chromatin domain. You can tweak this according to your project. See [MACS3 documentation](https://macs3-project.github.io/MACS/docs/callpeak.html) for more details.",
                    "fa_icon": "fas fa-chart-area"
                },
                "save_macs_pileup": {
                    "type": "boolean",
                    "description": "Instruct MACS3 to create bedGraph files normalised to signal per million reads.",
                    "fa_icon": "fas fa-save",
                    "default": true
                },
                "skip_peak_qc": {
                    "type": "boolean",
                    "fa_icon": "fas fa-fast-forward",
                    "description": "Skip MACS3 peak QC plot generation."
                },
                "skip_peak_annotation": {
                    "type": "boolean",
                    "fa_icon": "fas fa-fast-forward",
                    "description": "Skip annotation of MACS3 and consensus peaks with HOMER."
                },
                "skip_consensus_peaks": {
                    "type": "boolean",
                    "description": "Skip consensus peak generation, annotation and counting.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_bdgcmp": {
                    "type": "boolean",
                    "default": true,
                    "description": "Skip bdgcmp calculation with MACS3.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "save_macs_bdgcmp_intermeds": {
                    "type": "boolean"
                },
                "skip_edd": {
                    "type": "boolean",
                    "description": "Whether to skip EDD peak calling.",
                    "fa_icon": "fas fa-fast-forward",
                    "default": true
                },
                "edd_bin_size": {
                    "type": "string",
                    "description": "Bin size (Kb) for EDD peak calling. Autoestimated if left blank. See [EDD documentation](https://github.com/CollasLab/edd) for more details."
                },
                "edd_gap_penalty": {
                    "type": "string",
                    "description": "Gap penalty for EDD peak calling. Autoestimated by EDD if left blank. See [EDD documentation](https://github.com/CollasLab/edd) for more details."
                },
                "edd_num_trials": {
                    "type": "integer",
                    "description": "Number of Monte Carlo trials for EDD peak calling. See [EDD documentation](https://github.com/CollasLab/edd) for more details.",
                    "default": 10000
                },
                "edd_fdr": {
                    "type": "number",
                    "description": "FDR threshold for EDD peak calling. See [EDD documentation](https://github.com/CollasLab/edd) for more details.",
                    "default": 0.01
                },
                "genrich_narrow_gap": {
                    "type": "integer",
                    "default": 100,
                    "description": "Genrich narrow gap parameter.",
                    "fa_icon": "fas fa-hand-scissors"
                },
                "genrich_broad_gap": {
                    "type": "integer",
                    "default": 200,
                    "description": "Genrich broad gap parameter.",
                    "fa_icon": "fas fa-hand-scissors"
                },
                "save_genrich_pileup": {
                    "type": "boolean",
                    "description": "Instruct Genrich to save pileup files.",
                    "fa_icon": "fas fa-save",
                    "default": true
                },
                "save_genrich_pvalues": {
                    "type": "boolean",
                    "description": "Instruct Genrich to save p-values.",
                    "fa_icon": "fas fa-save",
                    "default": true
                },
                "save_genrich_intervals": {
                    "type": "boolean",
                    "description": "Instruct Genrich to save intervals.",
                    "fa_icon": "fas fa-save",
                    "default": true
                },
                "skip_genrich": {
                    "type": "boolean",
                    "description": "Skip Genrich peak calling.",
                    "fa_icon": "fas fa-fast-forward",
                    "default": true
                },
                "epic2_binsize": {
                    "type": "integer",
                    "default": 250,
                    "description": "Size of the windows to scan the genome in epic2. Bin size is the smallest possible island.",
                    "fa_icon": "fas fa-chart-area"
                },
                "epic2_fdr": {
                    "type": "number",
                    "default": 0.01,
                    "description": "In epic2 peak calling, remove all islands with an FDR above this cutoff.",
                    "fa_icon": "fas fa-chart-area"
                },
                "skip_dpeak": {
                    "type": "boolean",
                    "description": "Skip DANPOS2's dpeak peak calling.",
                    "fa_icon": "fas fa-fast-forward",
                    "default": true
                },
                "skip_dpos": {
                    "type": "boolean",
                    "description": "Skip DANPOS2's dpos peak calling.",
                    "fa_icon": "fas fa-fast-forward",
                    "default": true
                },
                "skip_encode_pipeline": {
                    "type": "boolean",
                    "description": "Skip peak calling with the ENCODE ChIP-seq pipeline.",
                    "fa_icon": "fas fa-fast-forward",
                    "default": true
                },
                "ctl_depth_ratio_threshold": {
                    "type": "number",
                    "default": 1.2,
                    "description": "For each sample, if the ratio of depth between input controls is higher than this, then a pooled input control will be used for all sample replicates.",
                    "fa_icon": "fas fa-balance-scale"
                },
                "skip_epic2": {
                    "type": "boolean",
                    "default": true,
                    "description": "Skip epic2 peak calling.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_consenrich": {
                    "type": "boolean",
                    "description": "Skip Consenrich (Genome-wide 'consensus' epigenomic state estimates and uncertainty metrics).",
                    "fa_icon": "fas fa-fast-forward",
                    "default": true
                },
                "skip_mace": {
                    "type": "boolean",
                    "default": true,
                    "description": "Skip MACE peak calling.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_macs3": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip MACS3 peak calling.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "seacr_peak_threshold": {
                    "type": "number",
                    "default": 0.05,
                    "fa_icon": "fas fa-align-justify",
                    "description": "SEACR specifies returns the top n fraction (between 0 and 1) of peaks based on total signal within peaks. This is only used if there are no controls included with the samples and if `--use_control` is `false`"
                },
                "seacr_norm": {
                    "type": "string",
                    "default": "non",
                    "fa_icon": "fas fa-align-justify",
                    "description": "SEACR normalization.",
                    "enum": ["non", "norm"]
                },
                "seacr_stringency": {
                    "type": "string",
                    "default": "stringent",
                    "fa_icon": "fas fa-align-justify",
                    "description": "SEACR stringency.",
                    "enum": ["stringent", "relaxed"]
                },
                "mace_kmer_size": {
                    "type": "integer",
                    "description": "Set the k-mer size for MACE peak calling. Kmer size [6,12] to correct nucleotide composition bias. kmerSize < 0.5*read_length. Larger KmerSize might make program slower. Set kmerSize = 0 to turn off nucleotide compsition bias correction. default=6. See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "mace_bin": {
                    "type": "integer",
                    "description": "Chromosome chunk size. Each chomosome will be cut into small chunks of this size. Decrease chunk size will save more RAM. default=100000 (bp). See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "mace_depth": {
                    "type": "integer",
                    "description": "Reference reads count (default = 10 million). Sequencing depth will be normailzed to this number, so that wig files are comparable between replicates. See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "mace_qcut": {
                    "type": "number",
                    "description": "phred scaled mapping quality threshhold to determine \u201cuniqueness\u201d of alignments. default=30. See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "mace_norm_method": {
                    "type": "string",
                    "description": "methods (\u201cEM\u201d, \u201cAM\u201d, \u201cGM\u201d, or \u201cSNR\u201d) used to consolidate replicates and reduce noise. \u201cEM\u201d = Entropy weighted mean, \u201cAM\u201d=Arithmetic mean, \u201cGM\u201d=Geometric mean, \u201cSNR\u201d=Signal-to-noise ratio. default=EM. See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna",
                    "enum": ["EM", "AM", "GM", "SNR"]
                },
                "mace_pvalue": {
                    "type": "number",
                    "description": "Pvalue cutoff for border detection and subsequent border pairing. default=0.05. See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "mace_max_dist": {
                    "type": "integer",
                    "description": "Maximum distance allowed for border pairing. default=100 (bp). See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "mace_fuzzy_size": {
                    "type": "integer",
                    "description": "Peaks (i.e. borders) located closely within this window will be merged. default=5 (bp). See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "mace_window_size": {
                    "type": "integer",
                    "description": "Background window size used to determine background signal level. default=100 (bp). See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "mace_signal_fold": {
                    "type": "number",
                    "description": "Minimum coverage signal used to build model (i.e. estimate optimal peak pair size). default=2.0. See the [MACE documentation](https://chipexo.sourceforge.net/) for more details.",
                    "fa_icon": "fas fa-dna"
                },
                "save_mace_intermeds": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to save the intermediate files from the MACE peak calling step. These include the MACE peak files in bedGraph format.",
                    "fa_icon": "fas fa-save"
                },
                "skip_denopa": {
                    "type": "boolean",
                    "description": "Skip deNOPA peak calling for ATAC-seq.",
                    "fa_icon": "fas fa-fast-forward",
                    "default": true
                }
            }
        },
        "process_skipping_options": {
            "title": "Process skipping options",
            "type": "object",
            "fa_icon": "fas fa-fast-forward",
            "description": "Options to skip various steps within the workflow.",
            "properties": {
                "skip_fastqc": {
                    "type": "boolean",
                    "description": "Skip FastQC.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_picard_metrics": {
                    "type": "boolean",
                    "description": "Skip Picard CollectMultipleMetrics.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_preseq": {
                    "type": "boolean",
                    "description": "Skip Preseq.",
                    "fa_icon": "fas fa-fast-forward",
                    "default": true
                },
                "deseq2_vst": {
                    "type": "boolean",
                    "description": "Use vst transformation instead of rlog with DESeq2.",
                    "help_text": "See [DESeq2 docs](http://bioconductor.org/packages/devel/bioc/vignettes/DESeq2/inst/doc/DESeq2.html#data-transformations-and-visualization).",
                    "fa_icon": "fas fa-dolly",
                    "default": true
                },
                "skip_plot_profile": {
                    "type": "boolean",
                    "description": "Skip deepTools plotProfile.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_plot_fingerprint": {
                    "type": "boolean",
                    "description": "Skip deepTools plotFingerprint.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_spp": {
                    "type": "boolean",
                    "description": "Skip Phantompeakqualtools.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_deseq2_qc": {
                    "type": "boolean",
                    "fa_icon": "fas fa-fast-forward",
                    "description": "Skip DESeq2 PCA and heatmap plotting."
                },
                "skip_igv": {
                    "type": "boolean",
                    "description": "Skip IGV.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_multiqc": {
                    "type": "boolean",
                    "description": "Skip MultiQC.",
                    "fa_icon": "fas fa-fast-forward"
                },
                "skip_qc": {
                    "type": "boolean",
                    "fa_icon": "fas fa-fast-forward",
                    "description": "Skip all QC steps except for MultiQC."
                }
            }
        },
        "partition_and_rfd_analysis_options": {
            "title": "SCAR-seq (partition) and OK-seq (RFD) analysis options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Options to adjust SCAR-seq and OK-seq analysis criteria.",
            "properties": {
                "partition_fragment_size": {
                    "type": "integer",
                    "description": "For single-end SCAR-seq or OK-seq data, the fragment size (in bp) to extend reads when calculating coverage for partition/RFD analysis. For paired-end data, coverage of intervals is calculated from the left point of a read pair to the right point.",
                    "default": 250
                },
                "partition_window_size": {
                    "type": "integer",
                    "description": "Window/bin size (in bp) for dividing chromosomes when calculating coverage for partition/RFD analysis in SCAR-seq or OK-seq experiments. See [this reference](https://www.science.org/doi/10.1126/science.aau0294#supplementary-materials) for more details.",
                    "default": 1000
                },
                "partition_step_size": {
                    "type": "integer",
                    "description": "Step size (in bp) for sliding window when calculating coverage for partition/RFD analysis in SCAR-seq or OK-seq experiments. Default is the same as `--partition_window_size` to create fixed-size book-ended bins instead of sliding windows.",
                    "default": 1000
                },
                "smooth_radius": {
                    "type": "integer",
                    "default": 30,
                    "description": "Smoothing radius for partition/RFD signal smoothing. Smoothing is computed across consecutive book-ended bins, performing a uniform blur considering the neighboring bins on each side. See [this reference](https://www.science.org/doi/10.1126/science.aau0294#supplementary-materials) for more details.",
                    "fa_icon": "fas fa-chart-line"
                },
                "derivative_radius": {
                    "type": "integer",
                    "default": 30,
                    "description": "Radius for [nonparametric derivative estimation](https://doi.org/10.18637/jss.v055.i02) of RFD signal to detect sharp transitions between neighboring bins for initiation zone detection. See [this reference](https://www.science.org/doi/10.1126/science.aau0294#supplementary-materials) for more details.",
                    "fa_icon": "fas fa-chart-line"
                },
                "zero_crossing_radius": {
                    "type": "integer",
                    "default": 1,
                    "description": "Radius for zero-crossing detection when defining replication initiation zones. See [this reference](https://www.science.org/doi/10.1126/science.aau0294#supplementary-materials) for more details.",
                    "fa_icon": "fas fa-chart-line"
                },
                "partition_rpm_cutoff": {
                    "type": "number",
                    "default": 0.3,
                    "description": "RPM (reads per million) cutoff for bins to be included in downstream partition analysis. Bins with normalized read-counts above this threshold will be included. See [this reference](https://www.science.org/doi/10.1126/science.aau0294#supplementary-materials) for more details.",
                    "fa_icon": "fas fa-filter"
                },
                "partition_plot_range": {
                    "type": "integer",
                    "default": 100,
                    "description": "Plot range for partition analysis visualization.",
                    "fa_icon": "fas fa-chart-area"
                },
                "partition_exclude_scaffolds": {
                    "type": "boolean",
                    "default": true,
                    "description": "Whether to exclude scaffolds from partition plots. Scaffolds are defined as chromosomes whose name begins with 'chrUn' or contains a dot ('.').",
                    "fa_icon": "fas fa-ban"
                },
                "partition_exclude_chromosomes": {
                    "type": "string",
                    "default": "chrX,chrY,chrM",
                    "description": "Comma-separated list of chromosomes to exclude from partition analysis. Typically sex chromosomes and mitochondrial chromosomes are excluded. See [this reference](https://www.science.org/doi/10.1126/science.aau0294#supplementary-materials) for more details.",
                    "fa_icon": "fas fa-ban"
                },
                "save_partition_intermeds": {
                    "type": "boolean",
                    "default": false,
                    "description": "Whether to save the intermediate files from the partition/RFD analysis step.",
                    "fa_icon": "fas fa-save"
                }
            }
        },
        "institutional_config_options": {
            "title": "Institutional config options",
            "type": "object",
            "fa_icon": "fas fa-university",
            "description": "Parameters used to describe centralised config profiles. These should not be edited.",
            "help_text": "The centralised nf-core configuration profiles use a handful of pipeline parameters to describe themselves. This information is then printed to the Nextflow log when you run a pipeline. You should not need to change these values when you run a pipeline.",
            "properties": {
                "custom_config_version": {
                    "type": "string",
                    "description": "Git commit id for Institutional configs.",
                    "default": "master",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "custom_config_base": {
                    "type": "string",
                    "description": "Base directory for Institutional configs.",
                    "default": "https://raw.githubusercontent.com/nf-core/configs/master",
                    "hidden": true,
                    "help_text": "If you're running offline, Nextflow will not be able to fetch the institutional config files from the internet. If you don't need them, then this is not a problem. If you do need them, you should download the files from the repo and tell Nextflow where to find them with this parameter.",
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_name": {
                    "type": "string",
                    "description": "Institutional config name.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_description": {
                    "type": "string",
                    "description": "Institutional config description.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_contact": {
                    "type": "string",
                    "description": "Institutional config contact information.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_url": {
                    "type": "string",
                    "description": "Institutional config URL link.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "help": {
                    "type": "boolean",
                    "description": "Display help text.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "description": "Method used to save pipeline results to output directory.",
                    "help_text": "The Nextflow `publishDir` option specifies which intermediate files should be saved to the output directory. This option tells the pipeline what method should be used to move these files. See [Nextflow docs](https://www.nextflow.io/docs/latest/process.html#publishdir) for details.",
                    "fa_icon": "fas fa-copy",
                    "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
                    "hidden": true
                },
                "fingerprint_bins": {
                    "type": "integer",
                    "default": 500000,
                    "description": "Number of genomic bins to use when calculating deepTools fingerprint plot.",
                    "fa_icon": "fas fa-dumpster",
                    "hidden": true
                },
                "email_on_fail": {
                    "type": "string",
                    "description": "Email address for completion summary, only when pipeline fails.",
                    "fa_icon": "fas fa-exclamation-triangle",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$",
                    "help_text": "An email address to send a summary email to when the pipeline is completed - ONLY sent if the pipeline does not exit successfully.",
                    "hidden": true
                },
                "plaintext_email": {
                    "type": "boolean",
                    "description": "Send plain-text email instead of HTML.",
                    "fa_icon": "fas fa-remove-format",
                    "hidden": true
                },
                "max_multiqc_email_size": {
                    "type": "string",
                    "description": "File size limit when attaching MultiQC reports to summary emails.",
                    "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
                    "default": "25.MB",
                    "fa_icon": "fas fa-file-upload",
                    "hidden": true
                },
                "monochrome_logs": {
                    "type": "boolean",
                    "description": "Do not use coloured log outputs.",
                    "fa_icon": "fas fa-palette",
                    "hidden": true
                },
                "multiqc_config": {
                    "type": "string",
                    "description": "Custom config file to supply to MultiQC.",
                    "fa_icon": "fas fa-cog",
                    "hidden": true
                },
                "validate_params": {
                    "type": "boolean",
                    "description": "Boolean whether to validate parameters against the schema at runtime",
                    "default": true,
                    "fa_icon": "fas fa-check-square",
                    "hidden": true
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/reference_genome_options"
        },
        {
            "$ref": "#/$defs/umi_extraction_and_deduplication_options"
        },
        {
            "$ref": "#/$defs/adapter_trimming_options"
        },
        {
            "$ref": "#/$defs/hard_trimming_options"
        },
        {
            "$ref": "#/$defs/alignment_options"
        },
        {
            "$ref": "#/$defs/spikein_options"
        },
        {
            "$ref": "#/$defs/multimapper_allocation_options"
        },
        {
            "$ref": "#/$defs/alignment_shifting_options"
        },
        {
            "$ref": "#/$defs/filtering_options"
        },
        {
            "$ref": "#/$defs/coverage_and_normalization_options"
        },
        {
            "$ref": "#/$defs/downsampling_options"
        },
        {
            "$ref": "#/$defs/tecounting_options"
        },
        {
            "$ref": "#/$defs/peak_calling_options"
        },
        {
            "$ref": "#/$defs/process_skipping_options"
        },
        {
            "$ref": "#/$defs/partition_and_rfd_analysis_options"
        },
        {
            "$ref": "#/$defs/institutional_config_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ],
    "properties": {
        "multiqc_logo": {
            "type": "string"
        },
        "multiqc_methods_description": {
            "type": "string"
        },
        "hook_url": {
            "type": "string"
        },
        "version": {
            "type": "boolean"
        },
        "pipelines_testdata_base_path": {
            "type": "string",
            "default": "https://raw.githubusercontent.com/nf-core/test-datasets/"
        },
        "local_testdata_base_path": {
            "type": "string",
            "default": "/maps/projects/dan1/data/Groth_group/SRP/crepas_testdata/"
        },
        "trace_report_suffix": {
            "type": "string",
            "default": "2025-08-03_12-45-27"
        }
    }
}
