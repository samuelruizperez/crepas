/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run grothlab/crepas -profile ku_sund_danhead,local_test_atacseq --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

params {

    config_profile_contact      = 'Samuel Ruiz-Pérez <samper@cancer.dk>'
    config_profile_name         = 'ATAC-seq test profile (for DAN System users)'
    config_profile_description  = 'Test dataset to check ATAC-seq analysis functions of the pipeline (for DAN System users)'

    // Input data
    input                       = params.local_testdata_base_path + 'samplesheets/local_test_atacseq_samplesheet.csv'
    read_length                 = 50

    // Genome references
    aligner                     = 'chromap'
    chromap_index               = params.local_testdata_base_path + 'references/Homo_sapiens/GRCh38/indices/chromap_0.2.6_index/GRCh38.primary_assembly.genome.index'
    fasta                       = params.local_testdata_base_path + 'references/Homo_sapiens/GRCh38/genome/fasta/GRCh38.primary_assembly.genome.fa.gz'
    gtf                         = params.local_testdata_base_path + 'references/Homo_sapiens/GRCh38/annotations/transcript_models/gtf/gencode.v47.annotation.gtf.gz'
    blacklist                   = params.local_testdata_base_path + 'references/Homo_sapiens/GRCh38/annotations/blacklists/bed/GRCh38.ENCODE_ENCFF419RSJ.Kundaje.Excludable.bed.gz'
    genome                      = 'GRCh38'

    // Other parameters
    allocate_n_multimappers     = 20
    allocation_method           = 'allo'
    save_align_intermeds        = true

    // For speed to avoid CI time-out
    fingerprint_bins            = 200
    skip_genes_plotprofile           = true

}